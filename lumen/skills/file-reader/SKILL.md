---
name: file-reader
version: 1.0.0
description: "Reads and processes local files"
level: 2
min_capability: tier-1
requires:
  connectors: [file]
---
Allows Neo to read, process, and analyze local files.
Uses the `file` connector with `read`, `write`, and `list` actions.
